import { viewerInteraction } from "./viewer-interaction";
import { browserIconSvg } from "./browser-icons";
/** Static viewer: page strings only ever enter textContent or form values. */
export const viewerHtml = `<!doctype html><html><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Browse — live browser</title><style>
:root{color-scheme:dark;font:13px system-ui,sans-serif}*{box-sizing:border-box}html,body{width:100%;height:100%;margin:0;overflow:hidden}body{display:flex;flex-direction:column;background:#0b0b0d;color:#d4d4d8}header{height:48px;flex:none;display:flex;gap:8px;padding:8px 16px;align-items:center;border-bottom:1px solid #ffffff12;background:#111113}button,summary{display:grid;place-items:center;width:28px;height:28px;border:0;border-radius:6px;background:transparent;color:#a1a1aa;cursor:pointer;flex:none}button:hover,summary:hover{background:#ffffff0c;color:#fafafa}button:focus-visible,summary:focus-visible,input:focus-visible{outline:2px solid #71717a;outline-offset:1px}svg{width:16px;height:16px;fill:none;stroke:currentColor;stroke-width:1.7;stroke-linecap:round;stroke-linejoin:round}input{flex:1;min-width:0;height:32px;margin:0 5px;padding:0 10px;border:1px solid transparent;border-radius:6px;background:#ffffff05;color:#e4e4e7;font:inherit}input:focus{background:#ffffff09}input::placeholder{color:#71717a}main{position:relative;display:grid;place-items:center;flex:1;min-height:0;min-width:0;overflow:hidden;padding:12px}#screen{display:block;flex:none;outline-offset:2px;outline:1px solid var(--border,#ffffff12);outline-offset:0;border-radius:6px}#screen{touch-action:none;user-select:none}#screen:not([data-frame]){visibility:hidden}#browser-keyboard{position:fixed;left:0;bottom:0;width:1px;height:1px;opacity:0;resize:none;padding:0;border:0;pointer-events:none}#status,#machine{position:absolute;bottom:8px;max-width:calc(50% - 20px);overflow:hidden;text-overflow:ellipsis;white-space:nowrap;padding:4px 8px;border-radius:5px;background:#111113dd;color:#a1a1aa;font-size:11px;pointer-events:none}#status{left:14px}#machine{right:14px}details{position:relative}summary{list-style:none}summary::-webkit-details-marker{display:none}.menu{position:absolute;z-index:5;right:0;top:38px;width:260px;padding:10px;border:1px solid #ffffff18;border-radius:10px;background:#18181b;box-shadow:0 12px 30px #0008}.menu button{width:100%;display:block;text-align:left;padding:0 8px;color:#e4e4e7}#identity{padding:8px;color:#a1a1aa;overflow-wrap:anywhere;font-size:12px;line-height:1.6}@media(max-width:420px){main{padding:6px}input{margin:0 2px;padding:0 6px}}

body{background:var(--background,#0b0b0d);color:var(--foreground,#d4d4d8)}header{background:var(--background,#111113);border-color:var(--border,#ffffff12)}button,summary{color:var(--muted-foreground,#a1a1aa)}button:hover,summary:hover,.menu button:focus-visible{background:var(--state-hover,#ffffff0c);color:var(--foreground,#fafafa)}input{margin:0;border:0;background:transparent;color:var(--foreground,#e4e4e7)}input:focus{background:transparent}.toolbar-group{display:flex;align-items:center;gap:4px;flex:none}.address-field{height:28px;display:flex;align-items:center;gap:4px;flex:1;min-width:0;margin:0;border:0;box-shadow:inset 0 0 0 1px var(--input,var(--border,#ffffff18));background:transparent;border-radius:6px}.address-field input{height:28px;font-size:12px;padding:4px 12px}.address-field input:focus-visible{outline:none}.address-field button{width:28px;height:28px;margin-right:2px}input::placeholder{color:var(--muted-foreground,#71717a)}.menu{background:var(--popover,#18181b);color:var(--popover-foreground,#e4e4e7);border-color:var(--border,#ffffff18);border-radius:8px;padding:4px;width:224px;max-width:calc(100vw - 24px)}.menu button{height:32px;font:inherit;color:inherit;border-radius:4px}.separator{height:1px;background:var(--border,#ffffff18);margin:4px -4px}#identity{font-size:11px;color:var(--muted-foreground,#a1a1aa)}#status,#machine{background:var(--popover,#111113dd);color:var(--muted-foreground,#a1a1aa)}

#status{pointer-events:auto}#status,#machine{display:flex;align-items:center;gap:6px}#machine svg{width:13px;height:13px;flex:none}#machine-label{overflow:hidden;text-overflow:ellipsis}.status-dot{width:6px;height:6px;border-radius:50%;flex:none;background:var(--warning,#eab308)}#status[data-state=ready] .status-dot{background:var(--success,#22c55e)}#status[data-state=error] .status-dot{background:var(--destructive,#ef4444)}.sr-only{position:absolute;width:1px;height:1px;padding:0;margin:-1px;overflow:hidden;clip:rect(0,0,0,0);white-space:nowrap;border:0}
#action-tooltip{position:fixed;z-index:100;max-width:calc(100vw - 16px);padding:6px 12px;border-radius:6px;border:1px solid var(--border,#ffffff18);background:var(--popover,#18181b);color:var(--popover-foreground,#e4e4e7);box-shadow:0 4px 12px #0003;font-size:12px;line-height:16px;pointer-events:none}#action-tooltip[hidden]{display:none}
@keyframes spin{to{transform:rotate(360deg)}}/* Keep the check and spinner at 16px; slightly reduce the denser copy glyph. */#copy-url svg{width:16px;height:16px}#copy-url[data-state=idle] svg{width:15px;height:15px}#copy-url[data-state=copied] svg{width:16px;height:16px}#copy-url[data-state=loading] svg{animation:spin 1s linear infinite}#copy-url:disabled{cursor:default}#copy-url,#copy-url:hover,#copy-url:active,#copy-url:focus-visible{background:transparent}#copy-url:hover,#copy-url[data-state=copied]{color:#fff}
</style></head><body>
<header aria-label="Browser navigation">
<div class="toolbar-group" role="group" aria-label="Navigation">
<button id="back" title="Back" aria-label="Back">${browserIconSvg("ArrowLeft")} </button>
<button id="forward" title="Forward" aria-label="Forward">${browserIconSvg("ArrowRight")} </button>
<button id="reload" title="Reload" aria-label="Reload">${browserIconSvg("RefreshCw")} </button>
</div>
<div class="address-field" role="group" aria-label="Address"><input id="url" aria-label="Page address" placeholder="Enter URL" spellcheck="false" autocapitalize="off"><button id="copy-url" title="Copy current URL" aria-label="Copy current URL">${browserIconSvg("Copy")}</button></div>
<div class="toolbar-group" role="group" aria-label="Page actions"><details><summary title="More options" aria-label="More options">${browserIconSvg("More")}</summary><div class="menu" role="menu" aria-label="More options"><button role="menuitem" data-maintenance="hard-reload">Hard reload</button><div class="separator" role="separator"></div><button role="menuitem" data-maintenance="clear-cookies" title="Signs you out of websites in this session profile">Clear cookies</button><button role="menuitem" data-maintenance="clear-cache">Clear cache</button><div class="separator" role="separator"></div><button role="menuitem" id="copy-session">Copy session ID</button></div></details></div>
</header>
<main id="viewport"><canvas id="screen" tabindex="0" aria-label="Live browser; select text, click, drag, type, paste and scroll"></canvas><div id="status" data-state="loading" title="Connecting…"><span class="status-dot" aria-hidden="true"></span><span id="resolution"></span><span id="status-message" class="sr-only" role="status">Connecting…</span></div><div id="machine" aria-label="Browser machine">${browserIconSvg("Machine")}<span id="machine-label"></span></div></main>
<script>
try{if(parent!==window){const root=parent.document.documentElement;const syncTheme=()=>{const styles=parent.getComputedStyle(root);for(const token of ['background','foreground','muted','muted-foreground','popover','popover-foreground','border','state-hover','ring','success','warning','destructive','primary','primary-foreground','input']){const value=styles.getPropertyValue('--'+token).trim();if(value&&CSS.supports('color',value))document.documentElement.style.setProperty('--'+token,value);}};syncTheme();const observer=new MutationObserver(syncTheme);observer.observe(root,{attributes:true});addEventListener('pagehide',()=>observer.disconnect(),{once:true});}}catch{}
if(new URL(location.href).searchParams.has('embedded'))document.body.classList.add('embedded');
const id=new URL(location.href).searchParams.get('id');const screen=document.querySelector('#screen'),status=document.querySelector('#status-message'),address=document.querySelector('#url');
let currentUrl='';let paused=false,acting=false,closed=false,vw=1280,vh=800,seq=0,cast=null,lastFrame=0,lastSocketFrame=0,inViewport=true;const clientId=crypto.randomUUID();const queue=[];
async function json(url,options){const r=await fetch(url,{...options,cache:'no-store'});const data=await r.json();if(!r.ok||data.error)throw Error(data.error||r.statusText);return data;}
const statusBadge=document.querySelector('#status');new MutationObserver(()=>{const text=status.textContent||'';statusBadge.title=text;statusBadge.dataset.state=/^(Live|Input completed|URL copied|Session ID copied)/.test(text)?'ready':/^(Connecting|Working|Resuming)/.test(text)?'loading':'error';}).observe(status,{childList:true,characterData:true,subtree:true});
const viewport=document.querySelector('#viewport');function fit(){const style=getComputedStyle(viewport);const w=viewport.clientWidth-parseFloat(style.paddingLeft)-parseFloat(style.paddingRight),h=viewport.clientHeight-parseFloat(style.paddingTop)-parseFloat(style.paddingBottom);const scale=Math.max(0,Math.min(w/vw,h/vh,1));screen.style.width=vw*scale+'px';screen.style.height=vh*scale+'px';}new ResizeObserver(fit).observe(viewport);
const metrics=window.browseMetrics={displayed:0,dropped:0,bytes:0,inputLatencyMs:0,maxInputQueue:0,startedAt:performance.now()};
let statusErrorUntil=0,pendingFrame=null,decodedFrame=null,decoding=false,paintScheduled=false;const paint=screen.getContext('2d',{alpha:false});
function acceptFrame(blob,frame,ack=()=>{}){metrics.bytes+=blob.size;if(pendingFrame){pendingFrame.ack();metrics.dropped++;}pendingFrame={blob,frame,ack};void decodeFrame();}
function drawFrame(){paintScheduled=false;const item=decodedFrame;decodedFrame=null;if(!item)return;const {bitmap,frame}=item;try{if(closed||document.hidden||!inViewport){metrics.dropped++;return;}if(screen.width!==bitmap.width||screen.height!==bitmap.height){screen.width=bitmap.width;screen.height=bitmap.height;}if(vw!==frame.width||vh!==frame.height){vw=frame.width;vh=frame.height;fit();}document.querySelector('#resolution').textContent=vw+' × '+vh;if(!screen.dataset.frame)fit();paint.drawImage(bitmap,0,0,screen.width,screen.height);screen.dataset.frame='true';lastFrame=Date.now();seq=frame.seq||seq;pageLoading=frame.loading===true;renderCopy();if(frame.url){currentUrl=frame.url;if(document.activeElement!==address)address.value=frame.url;}if(!acting&&Date.now()>statusErrorUntil)status.textContent='Live · '+vw+' × '+vh;metrics.displayed++;}finally{bitmap.close();item.ack();}}
async function decodeFrame(){if(decoding||!pendingFrame)return;decoding=true;const item=pendingFrame;pendingFrame=null;let bitmap;try{bitmap=await createImageBitmap(item.blob);if(closed||document.hidden||!inViewport){metrics.dropped++;bitmap.close();item.ack();return;}if(decodedFrame){decodedFrame.bitmap.close();decodedFrame.ack();metrics.dropped++;}decodedFrame={bitmap,frame:item.frame,ack:item.ack};if(!paintScheduled){paintScheduled=true;requestAnimationFrame(drawFrame);}}catch(e){bitmap?.close();item.ack();status.textContent='Could not decode browser frame.';}finally{decoding=false;if(pendingFrame)void decodeFrame();}}

function show(frame){if(!frame?.data)return;const bytes=Uint8Array.from(atob(frame.data),c=>c.charCodeAt(0));acceptFrame(new Blob([bytes],{type:'image/jpeg'}),frame);}
new IntersectionObserver(entries=>{inViewport=entries.some(e=>e.isIntersecting&&e.intersectionRatio>0);syncVisibility();void reportPresence();}).observe(viewport);
let lastTelemetry={at:performance.now(),displayed:0,bytes:0};
async function reportPresence(){try{const now=performance.now(),elapsed=Math.max(1,now-lastTelemetry.at);const sample={host:location.host,fps:(metrics.displayed-lastTelemetry.displayed)*1000/elapsed,mbps:(metrics.bytes-lastTelemetry.bytes)*8/elapsed/1000,inputLatencyMs:Math.min(60000,metrics.inputLatencyMs),displayed:metrics.displayed,dropped:metrics.dropped};lastTelemetry={at:now,displayed:metrics.displayed,bytes:metrics.bytes};await json('./presence',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({id,clientId,visible:inViewport&&!closed&&!paused&&!document.hidden&&inViewport&&Date.now()-lastFrame<15000,metrics:sample})});}catch{}}
void json('./viewer-info?id='+encodeURIComponent(id)).then(info=>{document.querySelector('#machine-label').textContent=info.hostLabel;}).catch(e=>{status.textContent=e.message;});
setInterval(()=>{if(inViewport&&!document.hidden)void reportPresence();},4000);document.addEventListener('visibilitychange',reportPresence);
function point(e){const r=screen.getBoundingClientRect();return{x:Math.min(vw-1,Math.max(0,(e.clientX-r.left)*vw/r.width)),y:Math.min(vh-1,Math.max(0,(e.clientY-r.top)*vh/r.height))};}
function openCast(){if(closed||paused||document.hidden||!inViewport||cast)return;const ws=new WebSocket((location.protocol==='https:'?'wss:':'ws:')+'//'+location.host+location.pathname.replace(/\\/viewer$/,'/cast')+'?id='+encodeURIComponent(id)+'&binary=1');cast=ws;let metadata;ws.onmessage=e=>{try{if(typeof e.data==='string'){const f=JSON.parse(e.data);if(f.error){status.textContent=f.error;ws.close();}else metadata=f;}else if(metadata){const frame=metadata;metadata=null;lastSocketFrame=Date.now();acceptFrame(e.data,frame,()=>{if(ws.readyState===WebSocket.OPEN)ws.send(JSON.stringify({ack:frame.seq}));});}}catch{}};ws.onclose=()=>{if(cast===ws)cast=null;if(!closed&&!paused&&!document.hidden&&inViewport)setTimeout(openCast,400);};ws.onerror=()=>ws.close();}
async function poll(){if(closed)return;try{if(!paused&&!document.hidden&&inViewport&&(!cast||cast.readyState!==WebSocket.OPEN))show(await json('./frame?id='+encodeURIComponent(id)+'&after='+seq));}catch(e){status.textContent=e.message;}finally{if(!closed)setTimeout(poll,1000);}}
function input(action){if(queue.length>=100){status.textContent='Input queue is full; wait for pending input.';return;}queue.push(action);if(!acting)void drain();}
async function drain(){if(!queue.length||closed)return;acting=true;const action=queue.shift();navigationPending=action.kind==='navigate'||action.kind==='history'||(action.kind==='maintenance'&&action.action==='hard-reload');renderCopy();status.textContent='Working…';try{let job=await json('./input',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({id,input:action})});while(job.status==='running'){await new Promise(r=>setTimeout(r,300));job=await json('./viewer-job?id='+encodeURIComponent(id)+'&job='+encodeURIComponent(job.id));}if(job.status!=='succeeded')throw Error(job.error||job.status);status.textContent='Input completed';}catch(e){queue.length=0;status.textContent=e.message;}finally{acting=false;navigationPending=false;renderCopy();if(queue.length)void drain();}}
for(const action of ['back','forward','reload'])document.querySelector('#'+action).onclick=()=>input({kind:'history',action});
function navigateAddress(){try{const text=address.value.trim();const url=new URL(text.includes('://')?text:'https://'+text);if(!['http:','https:'].includes(url.protocol)||url.username||url.password)throw Error('Enter an HTTP or HTTPS address without login details.');input({kind:'navigate',url:url.href});}catch(e){status.textContent=e.message;}}address.onkeydown=e=>{if(e.key==='Enter'){e.preventDefault();navigateAddress();}};
const copyUrlButton=document.querySelector('#copy-url');let copyReset,pageLoading=true,navigationPending=false,copied=false;
function renderCopy(){const state=pageLoading||navigationPending?'loading':copied?'copied':'idle';if(copyUrlButton.dataset.state===state)return;copyUrlButton.dataset.state=state;copyUrlButton.disabled=state==='loading';copyUrlButton.innerHTML=state==='loading'?${JSON.stringify(browserIconSvg("Loading"))}:state==='copied'?${JSON.stringify(browserIconSvg("Check"))}:${JSON.stringify(browserIconSvg("Copy"))};const label=state==='loading'?'Loading':state==='copied'?'Copied':'Copy current URL';copyUrlButton.dataset.tooltip=label;copyUrlButton.setAttribute('aria-label',label);}
function resetCopy(){clearTimeout(copyReset);copied=false;renderCopy();}
copyUrlButton.onclick=async()=>{try{if(!currentUrl)throw Error('No page URL to copy yet.');await navigator.clipboard.writeText(currentUrl);clearTimeout(copyReset);copied=true;renderCopy();copyReset=setTimeout(resetCopy,1500);status.textContent='URL copied';}catch(e){resetCopy();status.textContent='Could not copy URL: '+e.message;}};
renderCopy();addEventListener('pagehide',()=>clearTimeout(copyReset),{once:true});
document.querySelector('#copy-session').onclick=async()=>{try{await navigator.clipboard.writeText(id);status.textContent='Session ID copied';}catch(e){status.textContent='Could not copy session ID: '+e.message;}};
const tooltip=document.createElement('div');tooltip.id='action-tooltip';tooltip.role='tooltip';tooltip.hidden=true;document.body.append(tooltip);let tooltipTimer,tooltipTarget;
function hideTooltip(){clearTimeout(tooltipTimer);tooltip.hidden=true;tooltipTarget?.removeAttribute('aria-describedby');tooltipTarget=null;}
function showTooltip(target){hideTooltip();tooltipTimer=setTimeout(()=>{tooltipTarget=target;tooltip.textContent=target.dataset.tooltip;tooltip.hidden=false;const r=target.getBoundingClientRect(),t=tooltip.getBoundingClientRect();tooltip.style.left=Math.max(8,Math.min(innerWidth-t.width-8,r.left+(r.width-t.width)/2))+'px';tooltip.style.top=(r.bottom+t.height+8<innerHeight?r.bottom+4:Math.max(4,r.top-t.height-4))+'px';target.setAttribute('aria-describedby',tooltip.id);},400);}
for(const action of document.querySelectorAll('header button,header summary')){action.dataset.tooltip=action.dataset.tooltip||action.title||action.getAttribute('aria-label')||action.textContent.trim();action.removeAttribute('title');action.addEventListener('pointerenter',()=>showTooltip(action));action.addEventListener('pointerleave',hideTooltip);action.addEventListener('focus',()=>{if(action.matches(':focus-visible'))showTooltip(action);});action.addEventListener('blur',hideTooltip);action.addEventListener('click',hideTooltip);}
document.addEventListener('keydown',event=>{if(event.key==='Escape')hideTooltip();});addEventListener('pagehide',hideTooltip,{once:true});
const options=document.querySelector('details'),summary=options.querySelector('summary'),menu=options.querySelector('.menu');
for(const button of document.querySelectorAll('[data-maintenance]'))button.onclick=()=>{options.open=false;summary.focus();input({kind:'maintenance',action:button.dataset.maintenance});};
document.addEventListener('pointerdown',event=>{if(!options.contains(event.target))options.open=false;});
options.addEventListener('keydown',event=>{const items=[...menu.querySelectorAll('[role=menuitem]')];if(event.key==='Escape'){event.preventDefault();options.open=false;summary.focus();}else if(['ArrowDown','ArrowUp','Home','End'].includes(event.key)){event.preventDefault();options.open=true;const index=items.indexOf(document.activeElement);items[event.key==='Home'?0:event.key==='End'?items.length-1:(index+(event.key==='ArrowDown'?1:-1)+items.length)%items.length]?.focus();}});
${viewerInteraction}
window.addEventListener('pagehide' ,()=>{closed=true;cast&&cast.close();void reportPresence();});openCast();poll();
</script></body></html>`;
